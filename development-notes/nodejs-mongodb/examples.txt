
find, findOne
	- regex, $exists
	- $type
	- querying into an array
	- $all - all elements of an array must be present
	- cursors
	- $set / $unset
	- $pop, $push, $pull, $pushAll, $pullAll
	- $addToSet
	- upserts
	- multi data
	- removing data
	- skip, limit, sort
	- findAndModify
	- max / min (aggregation)